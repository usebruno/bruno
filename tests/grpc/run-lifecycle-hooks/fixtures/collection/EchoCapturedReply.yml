info:
  name: EchoCapturedReply
  type: grpc
  seq: 2

grpc:
  url: "{{host}}"
  method: /hello.HelloService/SayHello
  methodType: unary
  message:
    - title: message 1
      message: |-
        {
          "greeting": "{{capturedReply}}"
        }
