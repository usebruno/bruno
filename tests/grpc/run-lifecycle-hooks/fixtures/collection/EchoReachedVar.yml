info:
  name: EchoReachedVar
  type: grpc
  seq: 9

grpc:
  url: "{{host}}"
  method: /hello.HelloService/SayHello
  methodType: unary
  message:
    - title: message 1
      message: |-
        {
          "greeting": "reached-{{reached}}"
        }
