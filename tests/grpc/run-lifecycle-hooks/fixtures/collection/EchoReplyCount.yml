info:
  name: EchoReplyCount
  type: grpc
  seq: 4

grpc:
  url: "{{host}}"
  method: /hello.HelloService/SayHello
  methodType: unary
  message:
    - title: message 1
      message: |-
        {
          "greeting": "replies-{{replyCount}}"
        }
