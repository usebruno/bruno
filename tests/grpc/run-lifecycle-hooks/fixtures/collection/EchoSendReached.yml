info:
  name: EchoSendReached
  type: grpc
  seq: 17

grpc:
  url: "{{host}}"
  method: /hello.HelloService/SayHello
  methodType: unary
  message:
    - title: message 1
      message: |-
        {
          "greeting": "send-{{sendReached}}"
        }
