info:
  name: BrokenAfterCallEnd
  type: grpc
  seq: 10

grpc:
  url: "{{host}}"
  method: /hello.HelloService/SayHello
  methodType: unary
  message:
    - title: message 1
      message: |-
        {
          "greeting": "after-hook-throws"
        }

runtime:
  scripts:
    - type: grpc:after-call-end
      code: throw new Error('afterCallEnd exploded');
