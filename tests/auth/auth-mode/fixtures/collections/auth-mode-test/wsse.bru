meta {
  name: wsse
  type: http
  seq: 7
}

get {
  url: http://localhost:8081/ping
  body: none
  auth: wsse
}

auth:wsse {
  username: user
  password: password
}

script:pre-request {
  test("detects WSSE auth in pre-request script", function() {
    expect(req.getAuthMode()).to.equal("wsse");
  });
}

tests {
  test("detects WSSE auth in test script", function() {
    expect(req.getAuthMode()).to.equal("wsse");
  });
}
