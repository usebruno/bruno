meta {
  name: aws-sigv4
  type: http
  seq: 1
}

get {
  url: http://localhost:8081/ping
  body: none
  auth: awsv4
}

auth:awsv4 {
  accessKeyId: access-key
  secretAccessKey: secret-key
  sessionToken: 
  service: execute-api
  region: us-east-1
  profileName: 
}

script:pre-request {
  test("detects AWS SigV4 auth in pre-request script", function() {
    expect(req.getAuthMode()).to.equal("awsv4");
  });
}

tests {
  test("detects AWS SigV4 auth in test script", function() {
    expect(req.getAuthMode()).to.equal("awsv4");
  });
}
