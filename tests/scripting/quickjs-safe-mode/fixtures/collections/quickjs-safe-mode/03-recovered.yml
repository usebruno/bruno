info:
  name: 03-recovered
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:8081/ping

runtime:
  scripts:
    - type: tests
      code: |-
        test("scripts still run after the trap: fresh engine module serves", function() {
          bru.setVar('recovered', true);
          expect(bru.getVar('recovered')).to.equal(true);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
