meta {
  name: http-request-enum-prompt
  type: http
  seq: 1
}

post {
  url: http://localhost:8081/api/echo/json
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "env": "{{?Enter Environment|dev,*stage,prod}}",
    "regions": "{{?Enter Regions||us,eu,ap}}"
  }
}
