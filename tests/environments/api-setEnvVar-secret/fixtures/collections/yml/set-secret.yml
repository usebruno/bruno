info:
  name: set-secret
  type: http
  seq: 1

http:
  method: GET
  url: '{{baseUrl}}/ping'

runtime:
  scripts:
    - type: after-response
      code: |-
        bru.setEnvVar("apiToken", "NEW_VALUE_collection_env_e2e_42");
