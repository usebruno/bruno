info:
  name: seed-secret
  type: http
  seq: 3

http:
  method: GET
  url: '{{baseUrl}}/ping'

runtime:
  scripts:
    - type: after-response
      code: |-
        bru.setEnvVar("apiToken", "INITIAL_VALUE_collection_env_e2e_001");
