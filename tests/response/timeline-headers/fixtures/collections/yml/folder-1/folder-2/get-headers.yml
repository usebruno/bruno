info:
  name: get-headers
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8081/headers
  auth: inherit
  headers:
    - name: request-header-1
      value: request-header-value-1
    - name: request-header-2
      value: request-header-value-2

# A request item's scripts live under a top-level `runtime:` key (not `http:`) — this is what the
# app's serializer writes and what parseHttpRequest reads. Collection/folder yml use `request.scripts`.
runtime:
  scripts:
    - type: before-request
      code: |-
        req.setHeaders([
          { 'request-script-header-1': 'request-script-value-1' },
          { 'request-script-header-2': 'request-script-value-2' }
        ]);
