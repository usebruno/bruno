info:
  name: create-user
  type: http
  seq: 3

http:
  method: POST
  url: "{{host}}/users"
  body:
    mode: json
    json: |
      {
        "name": "John Doe",
        "email": "john@example.com"
      }
