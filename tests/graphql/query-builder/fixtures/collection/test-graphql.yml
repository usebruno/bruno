info:
  name: test-graphql
  type: graphql
  seq: 1

graphql:
  method: POST
  url: http://localhost:8081/api/graphql
  body:
    query: |-

    variables: |-
      {}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
