import styled from 'styled-components';

const StyledWrapper = styled.div`
  .copy-to-clipboard {
    cursor: pointer;
    opacity: 0.5;

    &:hover {
      opacity: 1 !important;
    }
  }
`;

export default StyledWrapper;
