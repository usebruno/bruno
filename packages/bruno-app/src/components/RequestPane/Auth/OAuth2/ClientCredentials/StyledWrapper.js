import styled from 'styled-components';

const Wrapper = styled.div`
  /* Inherit all Polaris OAuth2 styles from parent */
  /* This component uses the parent StyledWrapper styles */
  
  /* Additional specific overrides if needed */
  .select-wrapper {
    width: 100%;
    max-width: 300px;
  }
`;

export default Wrapper;
