import styled from 'styled-components';

const Wrapper = styled.div`
  /* Inherits styles from parent OAuth2 StyledWrapper */
  /* All Polaris form field, label, and select styles are inherited */
  
  .select-wrapper {
    width: 100%;
    max-width: 300px;
  }
`;

export default Wrapper;
