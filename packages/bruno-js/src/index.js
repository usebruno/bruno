const {
  ScriptRuntime
} = require('./script-runtime');

const {
  TestRuntime
} = require('./test-runtime');

const {
  VarsRuntime
} = require('./vars-runtime');

module.exports = {
  ScriptRuntime,
  TestRuntime,
  VarsRuntime
};
