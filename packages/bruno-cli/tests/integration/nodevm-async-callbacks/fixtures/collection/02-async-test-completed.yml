info:
  name: 02-async-test-completed
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/first"

runtime:
  scripts:
    - type: tests
      code: |-
        test('the awaited test callback finished before its run returned', function() {
          expect(bru.getVar('asyncTestRan')).to.equal('yes');
        });
