const { collectionSchema, itemSchema, environmentSchema, environmentsSchema } = require('./collections');
const { runsSchema } = require('./collections/run');

module.exports = {
  itemSchema,
  environmentSchema,
  environmentsSchema,
  runsSchema,
  runsSchema,
  collectionSchema
};
