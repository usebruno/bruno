# OAuth 1.0 Collection-Level Tests

API Collection to test OAuth 1.0 authentication configured at the collection level.

This collection demonstrates:
- OAuth 1.0 configured at the collection root (collection.bru)
- Requests inheriting OAuth 1.0 from collection
- Requests overriding collection-level OAuth 1.0 with request-specific configuration
