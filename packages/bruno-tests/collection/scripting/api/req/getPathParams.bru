meta {
  name: getPathParam
  type: http
  seq: 1
}

get {
  url: {{host}}/ping/:pathParam
  body: none
  auth: none
}

params:path {
  pathParam: 123
}

tests {npm test --workspace=packages/bruno-schema
  test("req.getPathParams()", function() {
    const pathParams = req.getPathParams();
    expect(pathParams[0].name).to.equal("pathParam");
    expect(pathParams[0].value).to.equal(123);
  });
}
