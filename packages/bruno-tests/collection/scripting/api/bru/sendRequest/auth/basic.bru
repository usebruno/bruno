meta {
  name: basic
  type: http
  seq: 8
}

get {
  url: {{host}}/ping
  body: none
  auth: none
}

tests {
  test("should test basic auth with sendRequest", async function() {
    const request = {
      url: 'https://httpbin.org/basic-auth/user/pass',
      method: 'GET',
      auth: {
        type: 'basic',
        basic: {
          username: 'user',
          password: 'pass'
        }
      }
    };

    await bru.sendRequest(request, (error, response) => {
      if(error) {
        console.log("basic auth", error)
      } else {
        console.log("basic auth", response)
      }
    });
  });
}
