meta {
  name: setCookie
  type: http
  seq: 2
}

get {
  url: {{host}}/ping
  body: none
  auth: inherit
}

script:post-response {
  const jar = bru.cookies.jar()
  
  jar.setCookie("https://testbench-sanity.usebruno.com", {
    key: "auth",
    value: "1234",
    path: "/path"
  })
}

settings {
  encodeUrl: true
}
