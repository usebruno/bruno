meta {
  name: clear
  type: http
  seq: 4
}

get {
  url: {{host}}/ping
  body: none
  auth: none
}

tests {
  test("should throw error (not yet implemented)", function() {
    const globalEnvVarList = bru.getGlobalEnvVarList();
    expect(() => globalEnvVarList.clear()).to.throw();
  });
}
