meta {
  name: digest
  type: http
  seq: 4
}

get {
  url: https://httpbin.org/digest-auth/auth/foo/passwd
  body: none
  auth: digest
}

auth:digest {
  username: foo
  password: passwd
}

assert {
  res.status: eq 200
  res.body.authenticated: isTruthy
}
