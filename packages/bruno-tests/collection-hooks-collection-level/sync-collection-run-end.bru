meta {
  name: sync-collection-run-end
  type: http
  seq: 1
}

get {
  url: {{host}}/ping
  body: none
  auth: none
}

tests {
  test("request should execute successfully", function() {
    // This request helps track request count for collectionRunEnd
    expect(res.getStatus()).to.equal(200);
  });
}

