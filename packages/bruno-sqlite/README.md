# Bruno SQLite

SQLite storage package for the Bruno API client.

## Development

```bash
# Build the package (bundles + emits type declarations)
npm run build --workspace=packages/bruno-sqlite

# Rebuild on change
npm run watch --workspace=packages/bruno-sqlite

# Run tests
npm run test --workspace=packages/bruno-sqlite
```
