name: 'Run Proxy E2E Tests - Linux'
description: 'Run proxy E2E tests on Linux'
runs:
  using: 'composite'
  steps:
    - name: Run Proxy E2E tests
      shell: bash
      run: |
        set -euo pipefail
        
        xvfb-run npm run test:e2e:proxy

    - name: Upload Playwright Report
      if: ${{ !cancelled() }}
      uses: actions/upload-artifact@v4
      with:
        name: playwright-report-proxy-linux
        path: playwright-report/
        retention-days: 30
